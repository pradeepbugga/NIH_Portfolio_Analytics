# Prompt Templates

The production prompt templates used for grant classification are not included in the public repository.

The classification pipeline loads prompt templates from this directory. This repository includes the complete pipeline architecture and evaluation framework, but omits the optimized production prompts.